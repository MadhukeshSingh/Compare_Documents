# DocDiff Pro — Python dependencies
# Install with: pip install -r requirements.txt

# ── Streamlit ──────────────────────────────────────────────────────────────
streamlit>=1.35.0

# ── PDF processing ─────────────────────────────────────────────────────────
PyMuPDF>=1.23.0          # fitz — PDF text extraction + rendering

# ── DOCX processing ────────────────────────────────────────────────────────
python-docx>=1.1.0       # Text extraction from .docx / .doc
mammoth>=1.6.0           # DOCX → clean HTML preview

# ── Image processing ───────────────────────────────────────────────────────
Pillow>=10.0.0           # PIL — highlight overlay rendering

# ── Diff / standard library (already included, listed for clarity) ─────────
# difflib — standard library, no install needed
# Streamlit Cloud system packages
# PyMuPDF and Pillow are pure Python/wheels — no system deps needed.
# If you add LibreOffice support later, add: libreoffice
